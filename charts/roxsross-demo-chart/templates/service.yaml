apiVersion: v1
kind: Service
metadata:
  name: {{ include "roxsross-demo-chart.fullname" . }}
  labels:
    {{- include "roxsross-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "roxsross-demo-chart.selectorLabels" . | nindent 4 }}
